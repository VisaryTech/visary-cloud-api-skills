{
  "model": {
    "schema": "assets/index/milestonefortaskregistry.json#model",
    "collection": true,
    "fields": {
      "id": "integer:int32",
      "hidden": "boolean",
      "title": "string nullable"
    }
  },
  "methods": [
    {
      "key": "GET /MilestoneForTaskRegistry",
      "cliShape": "python api.py -m get_milestone_for_task_registry",
      "summary": "get milestone for task registry"
    },
    {
      "key": "GET /odata/MilestoneForTaskRegistry",
      "cliShape": "python api.py -m odata_milestone_for_task_registry --odata-arg '$filter=<value>' --odata-arg '$select=<value>' --odata-arg '$expand=<value>' --odata-arg '$top=<value>' --odata-arg '$skip=<value>' --odata-arg '$orderby=<value>' --odata-arg '$count=<true|false>'",
      "summary": "odata milestone for task registry"
    },
    {
      "key": "GET /odata/MilestoneForTaskRegistry/$count",
      "cliShape": "python api.py -m odata_milestone_for_task_registry_count --odata-arg '$filter=<value>'",
      "summary": "odata milestone for task registry count"
    }
  ]
}
