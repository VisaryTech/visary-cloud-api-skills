interface:
  display_name: "Calendar API"
  short_description: "Work with VIS Calendar API via Swagger"
  default_prompt: "Use the Calendar API skill to read or change calendars, events, permissions, and calendar import/export operations through generated Swagger artifacts."
